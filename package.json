{
  "name": "history_microservice",
  "description": "A microservice that records the history.",
  "scripts": {
    "development": "npx nodemon server.js",
    "production": "npx forever start server.js",
    "stop_production": "npx forever stop server.js",
    "start": "node server.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "Min Seob Kim",
  "dependencies": {
    "express": "^5.2.1",
    "nodemon": "^3.1.14"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}